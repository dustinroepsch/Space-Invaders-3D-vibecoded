# wasm-bindgen 0.2.100+ requires reference-types to emit __wbindgen_externref_table_alloc.
# RUSTFLAGS is no longer set globally; native builds use CARGO_TARGET_X86_64_PC_WINDOWS_MSVC_RUSTFLAGS.
[target.wasm32-unknown-unknown]
rustflags = ["-C", "target-feature=+reference-types"]
